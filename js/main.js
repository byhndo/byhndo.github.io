const { createApp } = Vue
  createApp({
    data() {
      return {
        bg: 'bio'
      }
    }
  }).mount('#app')



Splitting();


$(document).ready(function() {
$( ".dot" ).delay(100).animate({top:"0"},
     3000, 'easeInOutBounce'

)
  });








